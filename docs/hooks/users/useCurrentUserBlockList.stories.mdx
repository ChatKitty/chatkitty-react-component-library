import { Canvas, Meta, ArgsTable } from "@storybook/addon-docs";

<Meta title="Hooks/Users/useCurrentUserBlockList" />

# <u>useCurrentUserBlockList</u>

Note: This hook does not make an initial request on mount.

```typescript
const { resource, isLoading, error, makeRequest } = useCurrentUserBlockList();
```

## Input

| Parameter | Type      | Required | Defaults | Description               |
| :-------- | :-------- | :------- | :------- | :------------------------ |
| `client`  | ChatKitty | Yes      | n/a      | ChatKitty client instance |

## Output

| Parameter     | Type                  | Description                                    |
| :------------ | :-------------------- | :--------------------------------------------- |
| `resource`    | UserBlockListItem[]   | Block list for current user                    |
| `isLoading `  | boolean               | Request loading state                          |
| `error`       | ChatKittyError        | If there's an error, it will be available here |
| `makeRequest` | `() => Promise<void>` | Callback to request or re-request              |
